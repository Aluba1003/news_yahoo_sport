name: News Bot

on:
  workflow_dispatch:   # 手動觸發
  schedule:
    - cron: "0 */3 * * *"   # 每 3 小時執行一次

permissions:
  contents: write   # 讓 GITHUB_TOKEN 有寫入 repo 的權限

jobs:
  run-news-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4 # 升級至 v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip' # ✅ 開啟快取，省去每次下載套件的時間

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create sources.yml from Secrets
        run: |
          if [ -z "${{ secrets.SOURCES_YML }}" ]; then
            echo "❌ SOURCES_YML secret is empty!"
            exit 1
          fi
          echo "${{ secrets.SOURCES_YML }}" > sources.yml

      - name: Run bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          python run_bot.py

      - name: Commit pushed.json back to repo
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          if [ -f pushed.json ]; then
            git add pushed.json
            if ! git diff --cached --quiet; then
              git commit -m "Update pushed.json [skip ci]" # ✅ 加入 [skip ci]
              # ✅ 先拉取最新變更，再把紀錄接在後面，取代原本危險的 --force
              git pull --rebase origin main
              git push origin HEAD:main
            else
              echo "No changes in pushed.json"
            fi
          fi
